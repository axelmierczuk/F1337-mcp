// Package tools implements the MCP tool handlers.
//
// Every tool that targets a sandbox takes an optional "sandbox" argument that
// overrides the sticky selection, and every result echoes the sandbox that
// actually served it.
//
// Implemented by milestone M2.
package tools
